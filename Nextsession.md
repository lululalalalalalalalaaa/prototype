# 다음 세션 할 일 (Nextsession.md)

지금까지(Stage 0~6 + 인프라)로 RAG **flow는 완성**되었고 전 과정이 eval로 검증됩니다.
아래는 그 위에서 이어갈 작업을, **왜 / 성공 기준**과 함께 우선순위 순으로 정의합니다.

## 현재 상태 (출발점)

- 검색 품질(**확장 골든셋 105, k=5**, 정제+구조청킹+이미지 후): Dense 0.811 → Hybrid 0.794 → **Rerank MRR 0.972**(hard 0.950).
- 그라운딩(전체 파이프라인): off-domain 기권 **1.000**, 응답 정확도 0.962, 과잉기권 0.026. **내용 질의 12/12**.
- 인덱스 37문서/912청크(본문452+표431+이미지29)/메타데이터. **public repo에 커밋·푸시**. 테스트 139 passed.
- ✅ 완료(이번 세션): 골든셋 46→105(내용 질의 포함), BM25 ablation(유지), 그라운딩 강화(0.867→1.000),
  출처(섹션명·위치)·토큰/비용·로깅, others 상대임계, README 배지, **HWP 정제 + 구조 인식 청킹 + 표 분리 + 이미지 vision**.
- ❌ 반려: rerank+recommend 호출 병합(품질 회귀, logging #33).

## 0. 인제스션 다음 — 표 셀 파편 reflow / 공유 템플릿 이미지  (선택)

- **왜**: 구조 청킹·표 분리·이미지는 완료. 남은 자투리: **≤3자 표 셀 파편**(표가 셀 단위 줄로 쪼개짐)을 표 내에서 reflow하면
  표 청크 가독성↑. 9·7문서 공통 템플릿 이미지(현재 제외)는 1회 설명해 공유 가능(현재는 unique만).
- **성공 기준**: rerank·그라운딩 비회귀, 표 청크 내 행 재구성.

---

## 1. hard 질의 개선  (남은 헤드룸)

- **왜**: hard MRR 0.925 — 지역 광역명(영남/호남/경기→수도권)·물 종류 경계에서 일부 실패. 여기가 다음 품질 표적.
- **무엇**: rerank 프롬프트에 도메인 힌트(지역 동의어·용수 종류 정의), 메타데이터(지역·기능단위) 활용. 난이도별
  분해로 정확한 실패를 짚어 개선. (참고: BM25는 net-neutral이라 여기엔 도움 안 됨)
- **성공 기준**: hard MRR > 0.925, 전체·그라운딩 비회귀.
- ✅ **완료된 관련 작업**: BM25 재검토(ablation: dense풀/hybrid풀 모두 0.962, net-neutral → **유지**),
  near-domain 그라운딩 강화(recommend 프롬프트 → 기권 0.867→**1.000**, 과잉기권 0.013).

## 2. Streamlit Cloud 배포  (운영)

- **왜**: 커밋·푸시는 완료(`cb61926`, public repo, `index/` 포함). 이제 실제 서비스로 띄울 차례.
- **무엇**: Streamlit Community Cloud에 repo 연결(BYOK — `OPENAI_API_KEY`는 secrets로). 재인덱싱은 로컬
  `build_index.py` 후 `index/` 갱신·커밋·푸시.
- **성공 기준**: 공개 URL에서 검색·추천·출처·세부정보 동작. "빌드→커밋→배포" 런북 1장.

## 3. rerank + recommend 호출 병합  ❌ 측정 결과 반려(2026-06-30)

- **시도**: 단일 LLM 호출(재정렬+추천+no_match) + 컨텍스트 다이어트(본문→근거 청크). 토큰 −74%·지연 −39%·호출 3→2 달성.
- **결과**: 품질 전면 회귀로 **되돌림** — rerank MRR 0.962→0.898(hard 0.752), 응답 0.974→0.795, off-domain 기권 1.000→0.800.
- **교훈(재시도 금지)**: rerank는 **넓고 싸게**(이름·15개), recommend는 **좁고 깊게**(본문·5개)로 컨텍스트 요구가
  근본적으로 다르다. 단일 호출은 다이어트하면 품질 붕괴, 풀본문이면 비용↑ — 3-call 구조가 품질/비용 frontier 위에 있다.
  (지연만 줄이려면 풀본문 병합으로 1라운드 절감은 가능하나 비용은 안 줄어 성공 기준 미충족.)

## 4. 검색 속도 — numpy 벡터화  (규모 확장 시)

- **왜**: 순수 파이썬 코사인은 수백 문서×청크(수천~수만 벡터)에서 느려진다(현재 37문서라 무문제).
- **무엇**: `retrieve.cosine_similarity`/`hybrid_rank`를 numpy 행렬연산으로 교체(임베딩은 이미 npz).
  eval-가드(동작 보존) 하에 진행.
- **성공 기준**: 목표 규모에서 질의 지연 < 목표치, eval 지표 **불변**.

## 5. 관측성 / 로깅 — 영속화  (원 Stage 7, 일부 완료)

- **왜**: ✅ 검색당 단계별 trace(타이밍·토큰·USD)·출처·콘솔 로깅·인앱 패널은 **완료**. 남은 건 **영속 로그**
  (질의·점수·비용을 파일/sqlite에 누적)와 주기적 eval 재측정 루틴.
- **무엇**: `pipeline.search`의 trace를 파일/sqlite에 append. "최근 N건 품질·비용" 집계 뷰.
- **성공 기준**: 운영 중 누적 로그로 비용·품질 드리프트 추적 가능.

## 6. 메타데이터 활용 — 필터 UI  (UX)

- **왜**: Stage 5에서 세부정보(기능단위·시스템경계·Climate change_Total 등)를 이미 인덱스에 precompute함.
- **무엇**: 지역/기능단위 등으로 결과 필터, 추천 카드에 핵심 수치(Climate change_Total) 노출.
- **성공 기준**: 필터·수치 표시가 정확히 동작(메타데이터 기반).

## 7. (옵션) floor·no_match 로직 정리

- **왜**: 그라운딩 측정 결과 LLM no_match가 완벽(1.000)하므로, 코사인 `similarity_floor`(0.40)는 이제
  '다른 유사 후보' 표시용으로만 남았다. 의미를 문서화하거나 단순화 여지.
- **성공 기준**: floor 역할이 명확히 문서화되거나, eval 비회귀로 단순화.

---

## 빠른 재개 메모

- 평가: `uv run python eval/run_eval.py --mode rerank --k 5` (품질) / `--mode answer` (그라운딩, 캐시됨).
- 인덱스 재생성: `uv run python scripts/build_index.py` (증분 — 변경분만 재임베딩/요약).
- 마무리 전 `npx cspell "**/*.{py,md,yaml,toml,json,jsonl}" --no-progress`로 점검.
- 상세 이력: `logging.md`, 설계 컨벤션: `CLAUDE.md`.
