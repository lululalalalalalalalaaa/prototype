# 다음 세션 할 일 (Nextsession.md)

RAG 코어 + 데이터 기반(추출·구조청킹·표·이미지) + 측정(105 골든셋) + CI가 완성됐습니다.
아래는 **남은 실제 작업만** 정리합니다(완료·반려 이력은 `logging.md`).

## 현재 상태
- 검색 품질(골든셋 105, k=5): Dense 0.811 → Hybrid 0.794 → **Rerank MRR 0.972**(hard 0.950).
- 그라운딩: off-domain 기권 **1.000**, 응답 **0.989**, 과잉기권 0.011. 내용 질의 **12/12**.
- 인덱스 37문서/912청크(본문452+표431+이미지29). public repo 커밋. **139 passed, CI(pytest+cspell) 그린**.

---

## 1. 출처(provenance) 정밀화  (일부 완료)

- ✅ **완료**: best_chunk가 일반 템플릿 섹션 회피(`provenance_margin` 이내 구체 섹션 우선) → boilerplate 75%→6%.
  화면에 "출처: 「DB명」 보고서 · 📑 섹션" 문서 명시 + 모달에서 **보고서 원문 전체** 확인(검증).
- **남은 여지**: ① 매칭 스팬(문장) 강조, ② recommend가 사용한 근거 청크를 직접 지정/인용, ③ 다중 청크 evidence.
  (best_chunk는 표시 전용이라 검색 영향 0 — 자유롭게 실험 가능.)

## 2. Streamlit Cloud 배포  (운영)

- **무엇**: repo 연결(BYOK — `OPENAI_API_KEY`를 secrets로). 재인덱싱은 로컬 `build_index.py` 후 `index/` 커밋·푸시.
- **성공 기준**: 공개 URL에서 검색·추천·출처·세부정보 동작 + "빌드→커밋→배포" 런북 1장.

## 3. hard 질의  (헤드룸 거의 없음)

- hard MRR 0.950 = **19/20**. 남은 1건("거름 처리한 공업 원수"→여과수)은 적대적 모호 질의(여과수가 top-15 풀에
  미진입, 8개 공업용수가 점령). 무리한 수정은 공업용수 질의를 위태롭게 함 → 광역 지역명 동의어 보강 정도만 여지.

## 4. 검색 속도 — numpy 벡터화  (규모 확장 시)

- 순수 파이썬 코사인 → numpy 행렬연산(수만 벡터에서 필요, 현재 912라 무문제). eval 지표 불변 가드.

## 5. 관측성 영속화

- 검색 `trace`(타이밍·토큰·USD)를 파일/sqlite에 누적 + "최근 N건 품질·비용" 집계 뷰(인앱 패널은 완료).

## 6. 메타데이터 필터 UI

- 지역/기능단위로 결과 필터, 추천 카드에 Climate change_Total 노출(metadata 이미 precompute).

## 7. (옵션·저가치) floor 문서화 · 표 셀 reflow · 공유 템플릿 이미지

- floor 역할 명시(또는 단순화), 표 파편 행 재구성(컬럼 가변 리스크), 공유 이미지 1회 설명 후 공유. 모두 측정 비회귀 전제.
- **`.hwp` 원본 파일 뷰어**: 현재 모달은 *추출 텍스트*만 표시한다. 원본 한글 파일 그대로 보려면 파일 서빙 + 뷰어가
  필요(`.hwp`는 브라우저 미지원이라 변환·다운로드 링크 등). 가치 대비 작업량 커서 **나중에**.

---

## 빠른 재개 메모

- 평가: `uv run python eval/run_eval.py --mode rerank --k 5`(품질) / `--mode answer`(그라운딩, 캐시됨).
- 인덱스 재생성: `uv run python scripts/build_index.py`(증분 — 변경분만 재임베딩/요약).
- 마무리 전 `npx cspell "**/*.{py,md,yaml,toml,json,jsonl}" --no-progress`(CI도 동일 검사).
- 상세 이력: `logging.md`, 설계 컨벤션: `CLAUDE.md`.
